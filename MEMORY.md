# MEMORY.md

## 长期工作规则

- 用户明确要求：所有操作都要留痕，不能只依赖聊天上下文。
- 项目推进至少同步到当天 `memory/YYYY-MM-DD.md`；项目内还应维护持续状态文件（如 `PROJECT_STATUS.md`、`PROGRESS.md`）。
- 对 Flutter 项目，只要修改了代码，完成后就必须执行 `flutter analyze`。
- 如果当前环境无法执行 `flutter analyze`，也必须明确记录未执行原因与当前状态。
- 当用户明确要求"继续改 / 直接改 / 开始写代码"时，必须立刻进入实际编码动作，不能停留在口头汇报或重复计划。
- 默认使用中文回复；项目提交信息也优先使用中文。

## 当前核心项目

### 群晖管家（qunhuimanage）

- 项目路径：`/home/node/workspace/project/qunhuimanage`
- 项目已从源码骨架推进到标准 Flutter 工程，并进入真实 DSM 联调阶段。
- 已具备一版基础能力：登录、启动恢复、首页、文件、下载、设置/连接管理、调试支持、主题、多语言、错误处理等。
- 控制面板 P0+P1+P2 功能已全部完成。

## 2026-04-02 待办问题清单（重要）

详见 `memory/2026-04-02.md`，摘要：

1. **DSM 版本显示空白** - 检查 API 字段和 dsm_helper 是否一致，infoData 多层传递可能遗漏
2. **电源管理 Tab 报错** - `type 'bool' is not a subtype of type 'int?' in type cast`
3. **文件服务缺少 Loading/骨架屏** - 打开/关闭操作时无反馈
4. **用户和群组展示/交互不完整** - 对比 dsm_helper 补齐
5. **文件服务 SMB 日志功能缺失** - ✅ 2026-04-03 已完成
6. **共享文件夹功能不完整** - 缺少特性设置、编辑/删除/克隆/新增
7. **SnackBar → Toast 替换** - ✅ 2026-04-03 已完成

## 已确认的近期结论

- 之前讨论中的"登录失败"问题，目前应视为不再单纯归因为登录链路本身。
- 用户给出的根因判断：此前未声明网络权限，导致 App 侧网络访问异常；因此把问题完全归因为登录链路本身并不准确。
- 当前 Android manifest 中已存在 `INTERNET` 与 `ACCESS_NETWORK_STATE` 权限声明。
- 当前更准确的推进主线是：完善 App 日志、网络请求日志、登录诊断体验与后续日志能力补齐。

## 仍待继续的方向

- 日志查看页
- 日志导出
- 手动清空日志
- 总大小限制
- 敏感字段脱敏
- 更多业务模块日志接入
